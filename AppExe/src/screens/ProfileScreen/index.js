import React from 'react';
import {View, Text} from 'react-native';

const ProfileScreen = ({navigation}) => {
  return (
    <View style={{padding: 20}}>
      <Text style={{padding: 20}}>Profile Screen</Text>
    </View>
  );
};

export default ProfileScreen;
